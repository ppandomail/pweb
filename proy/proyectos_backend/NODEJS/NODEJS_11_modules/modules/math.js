function sumar(x, y) {
  return x + y;
}

function restar(x, y) {
  return x - y;
}

function multiplicar(x, y) {
  return x * y;
}

function dividir(x, y) {
  return x / y;
}

module.exports = {
  sumar,
  restar,
  multiplicar,
  dividir,
};
